# Bootstrap only. All real config is served by the config-server (port 8888)
# from config-repo/academic-service.yml. Start the config-server FIRST.
spring:
  application:
    name: academic-service
  config:
    import: optional:configserver:http://localhost:8888
