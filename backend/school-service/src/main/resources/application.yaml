# Bootstrap only. All real config is served by the config-server (port 8888)
# from config-repo/school-service.yml. Start the config-server FIRST.
spring:
  application:
    name: school-service
  config:
    import: optional:configserver:http://localhost:8888
